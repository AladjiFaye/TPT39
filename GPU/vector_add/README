For the addition : with the mapping method, for N = 5000, I found that the GPU would have 8.3 MFLOPs/s; the CPU has better performances.

For N = 500000, the FLOPs for the GPU is equals 100 MFLOPs, and is higher than the CPU's FLOPs/s value.
